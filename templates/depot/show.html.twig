{% extends 'base.html.twig' %}

{% block title %}Depot{% endblock %}

{% block body %}
    <h1>Depot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ depot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ depot.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ depot.Address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('depot_index') }}">back to list</a>

    <a href="{{ path('depot_edit', {'id': depot.id}) }}">edit</a>

    {{ include('depot/_delete_form.html.twig') }}
{% endblock %}
